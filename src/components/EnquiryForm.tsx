'use client';
import { FormEvent, useState } from 'react';
import { ArrowUpRight, Check } from 'lucide-react';
export function EnquiryForm() { const [sent, setSent] = useState(false); const [loading, setLoading] = useState(false); function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setLoading(true); setTimeout(() => { setLoading(false); setSent(true); }, 650); } if (sent) return <div className="success-card"><span className="success-icon"><Check /></span><h3>Thanks — we’ve got it.</h3><p>Your enquiry is ready to be connected to your preferred inbox. Replace the placeholder email in the site configuration before launch.</p><a className="text-link" href="mailto:hello@yourbusiness.co.za">Email us directly <ArrowUpRight size={16} /></a></div>; return <form className="enquiry-form" onSubmit={submit}><div className="form-grid"><label>Name<input required name="name" placeholder="Your name" /></label><label>Business name<input required name="business" placeholder="Your business" /></label><label>Email<input required type="email" name="email" placeholder="you@example.com" /></label><label>Phone<input name="phone" placeholder="+27" /></label><label>Business type<select name="type" defaultValue=""><option value="" disabled>Select one</option><option>Café / restaurant</option><option>Professional services</option><option>Beauty / wellness</option><option>Property</option><option>Local services</option><option>Other</option></select></label><label>Preferred direction<select name="style" defaultValue=""><option value="" disabled>Select one</option><option>Minimal and clear</option><option>Warm and editorial</option><option>Bold and modern</option><option>Not sure yet</option></select></label></div><label>Tell us about the project<textarea required name="message" rows={5} placeholder="What does your business do, and what would you like the website to help with?" /></label><label className="check-label"><input required type="checkbox" /> <span>I understand this is an enquiry and not a commitment to purchase.</span></label><button className="button button-dark" disabled={loading}>{loading ? 'Sending…' : 'Send enquiry'} <ArrowUpRight size={18} /></button></form>; }

