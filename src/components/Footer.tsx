import Link from 'next/link';
import { ArrowUpRight, Instagram, Linkedin } from 'lucide-react';
import { navItems, siteConfig } from '@/config/site';
export function Footer() { return <footer className="footer"><div className="container footer-top"><div><Link href="/" className="brand footer-brand"><span className="brand-mark">{siteConfig.shortName}</span><span>{siteConfig.businessName}</span></Link><p className="footer-note">A better starting point for<br />small business websites.</p></div><div className="footer-links"><div><span className="eyebrow">Explore</span>{navItems.slice(1).map(item => <Link href={item.href} key={item.href}>{item.label}</Link>)}</div><div><span className="eyebrow">Get in touch</span><a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a><span>{siteConfig.location}</span><div className="socials">{siteConfig.instagram && <a href={siteConfig.instagram} aria-label="Instagram"><Instagram size={18} /></a>}{siteConfig.linkedin && <a href={siteConfig.linkedin} aria-label="LinkedIn"><Linkedin size={18} /></a>}</div></div></div></div><div className="container footer-bottom"><span>© {new Date().getFullYear()} {siteConfig.businessName}</span><span>Built for good work.</span><Link href="/contact">Start your website <ArrowUpRight size={14} /></Link></div></footer>; }

